
                        {/* Analytics Funnel */}
            <section className="bg-slate-900 rounded-[2rem] p-8 text-white shadow-2xl overflow-hidden relative">
              <div className="relative z-10"><Analytics applications={applications} /></div>
              <div className="absolute top-0 right-0 w-64 h-64 bg-indigo-500/20 blur-[100px] -mr-32 -mt-32 rounded-full"></div>
            </section>

            {/* Application Feed */}
            <div className="space-y-4">
              <h2 className="text-xl font-black text-slate-800 ml-2">Active Funnel</h2>
              {applications.length === 0 && <div className="text-center py-20 bg-white rounded-3xl border-2 border-dashed border-slate-200 text-slate-400 font-medium">No applications found. Paste a JD to start.</div>}
              {applications.map(app => (
                <div key={app.id} className="group bg-white rounded-3xl border border-slate-200 p-5 flex items-center gap-5 hover:border-indigo-300 hover:shadow-xl hover:shadow-indigo-500/5 transition-all duration-300">
                  
                  {/* Visual Match Component */}
                  <div className="hidden sm:block">
                    <MatchRing score={app.match_score} />
                  </div>

                  <div className="flex-1 min-w-0">
                    <div className="flex items-start justify-between">
                      <div>
                        <h3 className="font-bold text-slate-900 group-hover:text-indigo-600 transition-colors">{app.company}</h3>
                        <p className="text-xs font-medium text-slate-500">{app.role}</p>
                      </div>
                      <span className={`text-[10px] font-black uppercase tracking-tighter px-3 py-1 rounded-lg ${
                        app.status === 'offered' ? 'bg-emerald-100 text-emerald-700' :
                        app.status === 'interviewing' ? 'bg-amber-100 text-amber-700' :
                        app.status === 'rejected' ? 'bg-rose-100 text-rose-700' :
                        'bg-indigo-100 text-indigo-700'
                      }`}>
                        {app.status}
                      </span>
                    </div>

                    {/* AI Insights Chips */}
                    {(app.pros?.length > 0 || app.cons?.length > 0) && (
                      <div className="flex flex-wrap gap-2 mt-3">
                        {app.pros?.slice(0, 2).map(p => <span key={p} className="text-[10px] bg-slate-50 text-slate-600 px-2 py-0.5 rounded-md border border-slate-100">+ {p}</span>)}
                        {app.match_score > 7 && <span className="text-[10px] bg-emerald-50 text-emerald-600 px-2 py-0.5 rounded-md font-bold">Top Match</span>}
                      </div>
                    )}
                  </div>

                  <div className="flex flex-col gap-2 opacity-0 group-hover:opacity-100 transition-opacity">
                    <button onClick={() => handleEdit(app)} className="p-2 hover:bg-slate-100 rounded-lg text-slate-400 hover:text-indigo-600 transition-colors">
                      <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z" /></svg>
                    </button>
                    <button onClick={() => handleDelete(app.id)} className="p-2 hover:bg-rose-50 rounded-lg text-slate-300 hover:text-rose-600 transition-colors">
                      <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" /></svg>
                    </button>
                  </div>
                </div>
              ))}
            </div>